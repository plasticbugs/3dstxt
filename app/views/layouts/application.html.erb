<!DOCTYPE html>
<html>
<head>
  
<% if @title %>
<title><%= "3DSTXT.com - #{@title}" %></title>
<% else %>
<title>3DSTXT.com - Breaking the 16 Character Limit</title>
<% end %>
  <%= stylesheet_link_tag '/stylesheets/style.css' %>
	<link href='http://fonts.googleapis.com/css?family=Chewy' rel='stylesheet' type='text/css'>
	<link  href="http://fonts.googleapis.com/css?family=Maiden+Orange:regular" rel="stylesheet" type="text/css" >
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
<div id="container">
	<header>
		<%= link_to image_tag("3dtxtlogo.png", :alt => '3DSTXT Logo', :class => 'logo'), root_path %>
		<%= link_to image_tag("ribbon.png", :alt => 'Make your own 3DS txt!', :class => 'ribbon'), root_path %>
		<% if signed_in? %>
		<%= link_to "Sign out", signout_path, :method => :delete %><%= link_to "User Home", user_path(current_user.id) %>
		<% else %>
		<%= link_to "Create new account", signup_path %>
		<%= link_to "Sign in", signin_path %>
		<% end %>
		<%= debug(params) if Rails.env.development? %>
	</header>

	

<%= yield %>
</div>
</body>
</html>
